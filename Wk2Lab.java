import java.util.Scanner;

public class Wk2Lab {
    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in);
        CharCount(scan);
        IsPrime(scan);
        NthPrime(scan);
        StringRev(scan);
        IntOper(scan);
        IntQuizzer(scan);
    }

    static void CharCount(Scanner scan) {
        scan.nextLine();
    }
    
    static void IsPrime(Scanner scan) {
        scan.nextLine();
    }

    static void NthPrime (Scanner scan) {
        scan.nextLine();
    }

    static void StringRev(Scanner scan) {
        scan.nextLine();
    }

    static void IntOper(Scanner scan) {
        scan.nextLine();
    }

    static void IntQuizzer(Scanner scan) {
        
    }

}

